server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          #          uri: file:///${user.home}/configuration-repo // for windows users
          uri: file://${user.home}/configuration-repo
          clone-on-start: true
  application:
    name: config-server
